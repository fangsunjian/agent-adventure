# 第四阶段发展计划：编辑器升级与资源管理

## 🎯 总体目标

将HTML组件编辑器升级为专业级开发环境，提供更好的编辑体验和完善的资源管理功能。

## 📋 阶段划分

### Phase 4A: Monaco Editor集成 (优先级：高)

#### 目标
- 提供专业的代码编辑体验
- 支持语法高亮、代码补全、错误检查
- 实现全屏编辑和灵活的窗口管理

#### 技术方案
```typescript
// 使用 @monaco-editor/react
import { Editor } from '@monaco-editor/react';

interface MonacoEditorProps {
  language: 'html' | 'css' | 'javascript';
  value: string;
  onChange: (value: string) => void;
  isFullscreen?: boolean;
}
```

#### 功能特性
1. **语法高亮**：HTML/CSS/JavaScript完整语法支持
2. **智能补全**：代码自动补全和提示
3. **错误检查**：实时语法错误检测
4. **代码折叠**：支持代码块折叠/展开
5. **多光标**：多光标编辑支持
6. **全屏模式**：独立全屏编辑窗口
7. **主题切换**：支持明暗主题
8. **快捷键**：完整的编辑快捷键支持

#### UI设计
```
┌─────────────────────────────────────────────┐
│ [HTML] [CSS] [JavaScript] [📁资源]    [⛶全屏] │
├─────────────────────────────────────────────┤
│                                             │
│                                             │
│        Monaco Editor                        │
│        (with syntax highlighting)           │
│                                             │
│                                             │
├─────────────────────────────────────────────┤
│              Preview Panel                  │
│                                             │
└─────────────────────────────────────────────┘
```

### Phase 4B: 资源管理系统 (优先级：高)

#### 设计方案对比

##### 方案1: 本地资源库 (推荐)
```typescript
interface ResourceItem {
  id: string;
  name: string;
  type: 'image' | 'audio' | 'video' | 'json' | 'text';
  data: string; // base64 encoded
  size: number;
  uploadTime: number;
  tags: string[];
}

interface ResourceLibrary {
  resources: ResourceItem[];
  categories: ResourceCategory[];
  totalSize: number;
  maxSize: number; // 限制：比如50MB
}
```

**优点**：
- 无需服务器存储
- 数据随故事保存
- 离线可用
- 简单实现

**缺点**：
- 文件大小限制
- 故事文件可能变大

##### 方案2: 云端资源存储
**优点**：
- 支持大文件
- 资源共享
- 减小故事文件大小

**缺点**：
- 需要后端支持
- 网络依赖
- 成本考量

#### 推荐方案：混合模式
1. **小文件**（<1MB）：本地base64存储
2. **大文件**：提供外链引用方式
3. **资源库**：预置常用资源（图标、音效等）

#### 资源管理界面设计
```
┌─────────────────────────────────────────────┐
│ 📁 资源管理器                     [⬆上传] [➕新建] │
├─────────────────────────────────────────────┤
│ 📂 图片 (12)                               │
│   🖼️ background.jpg (256KB)                │
│   🖼️ character.png (128KB)                 │
│   🖼️ icon-sword.svg (4KB)                  │
│ 📂 音频 (3)                                │
│   🔊 bgm.mp3 (2.1MB) [外链]                │
│   🔊 click.wav (12KB)                       │
│ 📂 数据 (1)                                │
│   📄 config.json (2KB)                     │
├─────────────────────────────────────────────┤
│ 预览面板                                    │
│ 🖼️ [选中资源的预览]                         │
│ 📝 文件信息                                │
│ 🔗 引用代码: <img src="data:image...">      │
└─────────────────────────────────────────────┘
```

### Phase 4C: 编辑器界面优化

#### 布局改进
1. **分离式设计**：编辑器和预览可以独立调整大小
2. **标签页管理**：支持多文件同时编辑
3. **面板管理**：可折叠的资源面板、控制台面板
4. **快速操作**：常用操作的工具栏

#### 类AI Studio的界面特性
```
┌─────────────────────────────────────────────┐
│ Agent Adventure - HTML组件编辑器             │
├─────────────────────────────────────────────┤
│ [📁] [💾] [▶️] [🔧] [❓]                      │ <- 工具栏
├─────┬─────────────────────┬─────────────────┤
│文件树 │                     │                 │
│📁src │   Monaco Editor     │    Live         │
│├📄x.html│                   │   Preview      │
│├📄s.css│                    │                 │
│├📄a.js │                    │                 │
│📁res │                     │                 │
│├🖼️bg.jpg│                   │                 │
│└🔊c.wav│                    │                 │
├─────┼─────────────────────┼─────────────────┤
│控制台 │      调试输出        │    AI工具       │
│> log │                     │  🤖 已注册4个    │
└─────┴─────────────────────┴─────────────────┘
```

## 🛠️ 技术实现要点

### Monaco Editor集成步骤

1. **安装依赖**
```bash
npm install @monaco-editor/react
npm install monaco-editor
```

2. **配置类型定义**
```typescript
// 自定义类型定义for gameAPI
declare global {
  interface Window {
    gameAPI: {
      ai: { generate: (prompt: string) => Promise<string> };
      storage: { save: (key: string, data: any) => Promise<void> };
      // ... 其他API
    };
  }
}
```

3. **编辑器配置**
```typescript
const editorOptions = {
  theme: 'vs-dark',
  fontSize: 14,
  wordWrap: 'on',
  minimap: { enabled: true },
  automaticLayout: true,
  folding: true,
  lineNumbers: 'on',
  renderWhitespace: 'selection'
};
```

### 资源管理实现

1. **文件上传处理**
```typescript
const handleFileUpload = (file: File) => {
  if (file.size > MAX_FILE_SIZE) {
    // 提示使用外链
    return;
  }
  
  const reader = new FileReader();
  reader.onload = (e) => {
    const resource: ResourceItem = {
      id: generateId(),
      name: file.name,
      type: detectFileType(file),
      data: e.target.result as string,
      size: file.size,
      uploadTime: Date.now(),
      tags: []
    };
    
    addResourceToLibrary(resource);
  };
  reader.readAsDataURL(file);
};
```

2. **资源引用生成**
```typescript
const generateResourceReference = (resource: ResourceItem) => {
  switch (resource.type) {
    case 'image':
      return `<img src="${resource.data}" alt="${resource.name}">`;
    case 'audio':
      return `<audio src="${resource.data}" controls></audio>`;
    case 'json':
      return `fetch('${resource.data}').then(r => r.json())`;
  }
};
```

## 📈 开发优先级

### P0 (必须): Monaco Editor基础集成
- [ ] 安装Monaco Editor
- [ ] 替换现有的textarea编辑器
- [ ] 实现基础的语法高亮
- [ ] 添加全屏模式

### P1 (重要): 编辑器功能增强
- [ ] 代码自动补全
- [ ] 错误检查和提示
- [ ] 主题切换
- [ ] 快捷键支持

### P2 (重要): 资源管理
- [ ] 基础资源上传功能
- [ ] 资源预览
- [ ] 资源引用代码生成
- [ ] 资源管理界面

### P3 (优化): 高级功能
- [ ] 多标签页编辑
- [ ] 分离式预览窗口
- [ ] 资源库预置资源
- [ ] 代码片段库

## 🎯 用户体验目标

1. **编辑体验**：接近VS Code的编辑体验
2. **视觉设计**：现代化、专业的界面设计
3. **工作流程**：高效的开发-预览-测试流程
4. **学习曲线**：保持易用性的同时提供专业功能

## 📊 成功指标

- 编辑器响应性能：输入延迟 < 50ms
- 资源管理：支持至少50MB总存储
- 用户满意度：编辑体验明显改善
- 功能完整度：覆盖90%常见开发需求

---

这个计划将分阶段实施，确保每个阶段都能给用户带来明显的体验提升！