## 现在的目的： 现在游戏只能在开始设置游戏内容，实在太简单了，我要能自定义游戏设置，分享给别人玩，做社区。
### 我们现在有一个冒险游戏，可以点击开始游戏，设置各种游戏背景，然后让AI来进行游戏，请先理解目前程序架构和功能。
### 重要提示：仔细思考项目大概是多大规模，你的程序代码文件需要事先分类，比如按照每个页面，每个元素进行划分。按照高级软件架构师先进行规划。
### 提示：程序的基本功能和这次你进行的修改都添加到README.md文档，便于你下次开始工作时能回忆起来，记得加上时间戳
### 收到需求后需要和我确认工作内容，有不懂的地方请确认
1. 在程序下方添加导航栏，导航栏从左到右依次是*主页*，*分类*，*创作*，*我*，*临时*
2. 点击按钮会进入子页面，导航栏保持显示状态。简单来说类似youtube的手机app
3. 暂时把我们现在的主页放到*临时*页面
4. *主页*和*分类*里面暂时使用卡片列表，先做UI，实际功能后期填充
   1. 我上传了一个截图。里面有卡片的详细信息。我们的卡片与他类似包含左侧图片，右侧是创作者名字，故事标题，简介
   2. 分类页面顶部有搜索栏，搜索栏的下方有各种分类tag按钮，类似youtube手机app顶部的分类按钮
   3. 预先填充一些测试卡片，便于测试
5. 我们先做*创作*页面，这个页面用来创作故事
   1. 顶部导航栏左侧是关闭按钮，右侧是*发布*按钮
   2. 上方有导航栏，分别为*基本信息*，*资料库*，*剧情设定*
   3. *基本信息*里面包含*标题*，*故事简介*，*可见性*（选项：*公开*/*非公开*），*分类*
   4. *资料库*里面有*资料卡*列表
      1. *资料卡*列表中第一个项目是*添加*，点击后进入*资料卡*创建页面，里面有保存和取消
      2. *资料卡*列表里面是*资料卡*简介，包含*名字*，*类别*，*内容*预览,点击进入*资料卡*编辑页面
      3. *资料卡*创建和编辑页面类似，包含*类别*（包含*角色*，*位置*，*任务*）
   5. *剧情设定*里有*开场白*，选择开场白发言人是“旁白”还是*角色*，*角色*从资料库里拉取，“旁白”是固定选项，没有*角色*也可选择
6. 通过*创作*页面*发布*以后的*公开*内容可以在*主页*，*分类*里面找到。由用户制作的*公开*与*非公开*内容都可以在*我*的页面里找到并编辑。


1. *创作*按钮点击后弹出现在的*创作*页面遮住导航栏，点击关闭或者*发布*回到之前的页面
2. 接下来开始开发实际功能。先完善*创作*页面。
   1. 退出时弹出提示：是否放弃保存，并建议如果需要临时保存可以先发布为*非公开*
   2. 发布时检查*标题*，*故事简介*，*开场白*是否填写
   3. *资料库*的增删改查
      1. 点击添加资料库打开新窗口，左上是关闭按钮，点击提示保存或不保存退出，右边是保存按钮
      2. 增加一排类别按钮，点击可以快速找到所需类别。
      3. *资料库*里包含*名字*，*类别*，*内容*，*关键字*，最下方有红色按钮*删除*，点击时弹出确认对话框
      4. *类别*包含*角色*，*地点*，*道具*，*任务*
      5. *内容*最大1000字，添加当前字数和最大字数
      6. 增加一个隐藏的id,用于在其他地方做引用
   4. *剧情设定*里面增加*背景设定*和*开场选项*
   5. *背景设定*的下方增加一个*插入*按钮，点击弹出对话框，里面是*资料库*中的名字列表，点击添加到*背景设定*文本框中，渲染对话框时显示一个特殊样式的名字，用户删除时直接整个删除。比如说我在*资料库*里更改了某个角色的名字，*背景设定*里面也会同时改变。如果你有更好方法可以按照你的方法做。
   6. *开场白*与上述*背景设定*一样，也要*插入*资料库引用功能
   7. *发布*功能现在需要持久化。发布好了以后我真的可以删除修改，并在*我*页面看到

1. 先做些小修改，故事创建和编辑页面的*基本信息*页面增加*封面*，可以填入一个自定义图片url，预览卡片的封面使用这个自定义图片地址
2. 测试卡片内容和目前没有填充*资料库*，*背景设定*，*开场白*，*开场选项*，*开场白发言人*。首先重新生成测试卡片，让他们看起来像真人创作的。
3. 接下来开发游玩的部分。在*主页*和*分类*点击卡片直接进入*游戏界面*。
   1. *游戏界面*目前照搬*临时*页面的内容，然后开始修改。
   2. 开始游戏的逻辑稍有复杂：
      1. 如果这是第一次玩，那么直接开始新游戏，已经不需要以前的新游戏对话框了，直接从我们创建的故事卡中读取。
      2. 点击顶部*开始新游戏*后，让用户确认未保存游戏会丢失
      3. 当然之前的欢迎页面，就是包含"Gemini 冒险"的页面不需要了
   3. *游戏界面*页面中右上角的设置按钮，Drak Mode/Light Mode切换按钮移动到*我*的页面里
   4. *游戏界面*页面的背景使用游戏卡所设置的图片
4. *我*的页面里增加最近游玩分类，可以继续游玩或删除
5. 忘了说，把最新的开发进度写进readme

1. 游戏页面做成一个新页面，不要出现在tab上，和create一样，点击进行游玩。
2. 列表中的图片如果找不到就显示没有图片，加载成功了再替换。游戏页面的背景如果找不到图片就不显示。
3. 对话框的背景还是黑色的，先改成和游戏选项面板一样的半透明
4. "What do you do next?"这行字占用了很大的显示面积，放到别的位置去

1. 阅读readme.md和项目代码，理解目前项目进度。
2. 游戏的背景设定，开场白中，会有一些占位符，用{{}}包裹，比如{{user}}，增加一个功能，在开始游戏时弹出对话框询问玩家这里应该填入什么，支持追加描述比如：{{user:玩家姓名}}

1. 设置里增加一个参数能全局缩放UI。UI界面在我的浏览器里显得特别大
2. 游戏界面的消息窗口背景是黑色，我看不见图片了，修改成半透明
3. 设置里增加一个参数让我能调节透明度
4. 滚动条的颜色我想也能设置成主题颜色，现在我用的默认Dark Mode，但是在edge里滚动条是白色
5. 我希望在游戏途中也能快速更改设置，和*我*的界面右上角的设置完全相同。
6. 我的LLM设置并没有持久化，重新回到游戏以后设置全都重置了。
7. 我收到了这么一个错误，但是我随便调节一下温度，错误就消失了
```
Custom provider API error: 400 - {"error":{"message":"Expected temperature to be a number, received \"0.8\"","code":400,"metadata":{"provider_name":null}}}
```
8. 创作界面打开时下方的导航栏会被遮挡，我希望游戏界面也如此
9. 在*我*的界面删除游戏进度和故事的时候需要有确认提示框

1. 显示bug，界面缩放，界面透明度设置，刚打开时只显示一个%符号，没有显示100%，拖动以后开始表示数字。
2. 透明度我想最低能到0%
3. 透明度现在是控制全局透明度，我只想让他控制游戏界面透明度，其他界面，对话框不受影响。这个设置名字也改成“对话窗口透明度”
4. 对话框中我的话是蓝色透明，AI的话是黑色不透明，AI的话也弄成透明的
5. AI发来的文本经常有markdown，我设置的开场白偶尔也有markdown，能支持markdown渲染吗

1. 设置透明度为0%时，背景仍然是毛玻璃效果，能设置他的透明度吗
2. 对话框我还是感觉我说的话和AI说的话透明度不一样，感觉我说的话背景更深，Dark Mode和Light Mode都一样，我说的话既然是蓝色（还是紫色？），那给AI说的话也设置一个搭配点的颜色
3. 对话框进行修改
   1. 原本表示“接下来你做什么？”的地方，改成“接下来”，后面是选项（横着并列）
   2. 选项是“说”，“做”，“发生了”，选择了以后下面文本框里的提示也跟随改变
   3. 点击发送按钮时，自动加上：{{user}}说“说的内容”，{{user}}做的内容，或者直接发送发生的事情
   4. 如果没有设置{{user}}，就把{{user}}换成“我”

现在又回到底部有黑边，顶部和状态栏重叠的情况了。
这样吧，边框我们不要了。我看了几个别的app，其实大家都没有边框，而且圆角也不好适配市面上所有带圆角的手机
游戏界面现在是一个模态对话框。其实我们不是很需要模态对话框，周围会有黑边，降低沉浸感。
给你看个别的app，注意观察他的底部导航栏，顶部状态栏，按钮都是怎么处理的，大多数流行app都是这么个样式

1. 新建故事和编辑故事和资料卡的添加和资料卡的编辑对话框的页面并不是模态，点击对话框外会提示是否退出，需要修改成系统设置对话框一样的真模态。
2. 系统设置界面导入导出按钮名字，英文名改成"Import""Export"，因为单词过多会导致按钮变高，影响观感
3. 新建故事和编辑故事界面除了“发布”按钮以外，增添导出和导入功能
4. 资料库中资料卡类别增加“设定”和“自定义”，选择“自定义”以后出现新的文本框，要求输入自定义的名称
5. 资料卡预览里面的类别即使是中文环境也显示英文“Character”“Location”之类的，中文环境改成中文
6. 故事卡上面的按钮在中文环境显示也是“Edit”“Delete”，修改成中文

1. 我现在想要增加数据库了，你看到我们是个社区项目，需要集成用户的注册登录删除，故事的管理等复杂功能，希望你给我个可行的完整计划
2. 我刚注册了Supabase，有自己的API地址和KEY，你说能用不？
3. 后台管理是个大麻烦，尤其是在AI Studio里，我是不是开两个窗口，建两个项目一起干会比较好？

1. 对话途中经常打开总结窗口显示黑屏，整个界面都黑屏。控制台报错如下，不知道有没有关系：
```
optogbhbrfvxqrurqzml.supabase.co/auth/v1/token?grant_type=refresh_token:1  Failed to load resource: the server responded with a status of 400 ()Understand this error
hook.js:608 AuthApiError: Invalid Refresh Token: Refresh Token Not Found
    at handleError2 (@supabase_supabase-js.js?v=016f3309:4891:9)
    at async _handleRequest2 (@supabase_supabase-js.js?v=016f3309:4932:5)
    at async _request (@supabase_supabase-js.js?v=016f3309:4916:16)
    at async @supabase_supabase-js.js?v=016f3309:6815:16
    at async @supabase_supabase-js.js?v=016f3309:4711:26
overrideMethod @ hook.js:608Understand this error
SummaryPanel.tsx:71 Uncaught TypeError: Cannot read properties of undefined (reading 'map')
    at SummaryPanel.tsx:71:49
    at Array.map (<anonymous>)
    at SummaryPanel (SummaryPanel.tsx:62:27)
    at Object.react_stack_bottom_frame (react-dom_client.js?v=016f3309:17424:20)
    at renderWithHooks (react-dom_client.js?v=016f3309:4206:24)
    at updateFunctionComponent (react-dom_client.js?v=016f3309:6619:21)
    at beginWork (react-dom_client.js?v=016f3309:7654:20)
    at runWithFiberInDEV (react-dom_client.js?v=016f3309:1485:72)
    at performUnitOfWork (react-dom_client.js?v=016f3309:10868:98)
    at workLoopSync (react-dom_client.js?v=016f3309:10728:43)Understand this error
```
2. 系统设置按钮现在放在游玩界面，还是放到*我*的页面里
3. 游玩界面的关闭按钮移动到最左边，并改成返回，这样用户更加容易理解
4. LLM设置移动到最右侧
5. 提供对话框气泡的透明度选项
6. 把这个新增的气泡的透明度选项和原来的透明度选项都放到LLM设置里，LLM设置改名为游戏设置，这样更易懂

1. 我的程序之前非常稳定，现在在手机上上下滑动时会出现滚动条，整个程序会被滑动，Profile界面的列表甚至会被滑动的无法恢复初始位置
2. 我现在找了一个和我的手机屏幕尺寸1：1的图片做测试，但是上下仍有黑边，参考3.jpg