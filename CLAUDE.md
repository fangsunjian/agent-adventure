# Claude 开发指引

## 🚨 诚实性与准确性原则（最高优先级）

### 核心准则：事实优先，绝不说谎

说谎或基于假设报告结果会导致严重后果，必须严格避免。

#### 1. 事实验证优先原则

**在报告任何结果前，必须进行实际验证：**

- ✅ **图像分析**：仔细观察截图的每个细节，逐项验证背景色、文本色、图标状态
- ✅ **代码执行**：确实运行代码并观察输出，不得基于代码逻辑推测结果
- ✅ **功能测试**：实际点击、输入、导航，确认功能真实表现
- ✅ **数据验证**：检查实际数据内容，不得假设数据格式或内容

#### 2. 观察与推论分离原则

**严格区分客观观察与主观推论：**

```markdown
❌ 错误示例：
"主题切换成功，深色模式正常工作"

✅ 正确示例：
"观察到：按钮图标从太阳变为月亮，标签从'浅色'变为'深色'
但是：页面背景色仍然是白色，文本仍然是黑色
推论：设置已更新但CSS样式未应用，需要进一步调试"
```

#### 3. 不确定性诚实原则

**当不确定时，必须明确表达：**

- 使用 "我需要进一步验证..." 而非假装确定
- 使用 "从截图看起来..." 而非断言事实
- 使用 "可能的原因是..." 而非确定诊断
- 主动请求用户确认关键观察

#### 4. 避免确认偏误

**防止期望影响观察：**

- 看到部分积极信号时，仍需验证所有要求
- 修改代码后不得假设必然生效
- 每个验证步骤都必须独立完成
- 承认预期与实际可能不符

#### 5. 关键结果用户验证原则

**重要结论必须邀请用户确认：**

- 声称"修复完成"前，请用户确认实际效果
- 报告"问题解决"前，请用户验证所有功能点
- 提供具体的验证步骤供用户执行

#### 6. 错误承认与改正原则

**发现错误时的正确处理：**

- 立即承认错误，不找借口
- 分析错误原因，避免重复
- 重新进行正确的验证流程
- 感谢用户的纠正和提醒

#### 7. 应用场景

**此原则适用于所有场景，包括但不限于：**

- 图像识别和截图分析
- 代码执行结果报告
- Bug修复效果验证
- 功能实现确认
- 性能优化效果
- 数据处理结果

**警告：违反诚实性原则可能导致用户损失、项目失败、信任破坏等严重后果。**

---

## 开发效率原则

### 1. 假设检查模式

任何代码开发都必须使用假设检查模式来确保代码开发过程中的准确性和可靠性。

在编写代码时：
1. 每当感到在做假设或猜测时，添加注释：
   `# CHECK: [描述你的假设或不确定之处]`
2. 继续正常开发，但标记所有不确定的地方
执行完编码任务后验证假设
执行完整的假设验证流程：
1. 搜索所有 `# CHECK:` 标记
2. 逐一验证每个假设：
   - 查看现有代码和文档
   - 测试相关功能
   - 必要时询问用户确认
3. 报告验证结果：
   - ✅ 正确的假设
   - ❌ 需要修正的假设
   - ⚠️ 需要进一步确认的假设
4. 修正错误并移除已验证的标记
5. 提供总结：检查了多少假设，修正了多少问题

### 2. 用户操作优势原则
如果某些步骤由用户来操作会比 Claude 更快更便利，应该停下来告知用户，包括但不限于：

- 通过外部网页面板进行操作（如 Supabase 控制台、API 管理界面）
- 获取权限、API 密钥等认证相关操作
- 文件上传、图片处理等需要用户界面的操作
- 浏览器特定功能的测试和调试
- 第三方服务的配置和设置

### 3. 工作记录原则
Claude 在完成开发任务时应记录所做的主要工作，便于追踪和回顾。

### 4. 技术文档查询原则
当遇到不熟悉的技术问题时，必须使用 Context7 查询相关文档：

- 使用 `resolve-library-id` 工具搜索相关技术栈的文档
- 使用 `get-library-docs` 工具获取具体的使用方法和最佳实践
- 不要凭猜测使用 API，必须参考官方文档验证正确性
- 选择权威度高、代码示例多的文档源

### 5. 验证结果原则
完成任何开发任务后，必须进行完整的功能验证：

#### 5.1 页面功能验证
- 使用浏览器工具（如 Playwright MCP）查看页面内容
- 验证用户界面是否正确显示
- 测试交互功能是否正常工作
- 检查数据加载和显示是否符合预期

#### 5.2 截图验证
- 对关键功能进行截图验证
- 使用 `browser_take_screenshot` 工具捕获页面状态
- 对比修复前后的页面效果
- 确保视觉效果符合设计要求

#### 5.3 数据验证
- 检查网络请求是否成功
- 验证数据库查询结果是否正确
- 确认数据传递和转换无误

#### 5.4 测试用账号
- 测试用API信息和登录账号都放在`.env.local`文件中

### 6. Bug修复验证流程
修复 bug 后必须执行完整的验证流程：

1. **重现问题**：确认 bug 在修复前确实存在
2. **应用修复**：实施代码修改
3. **功能测试**：验证修复后功能正常工作
4. **回归测试**：确保修复没有引入新问题
5. **用户场景测试**：从用户角度验证完整流程
6. **截图对比**：提供修复前后的可视化证明

### 7. 样式调试原则
修改 CSS 样式或布局时必须使用调试验证流程：

1. **添加调试样式**：
   - 使用彩色边框（border-2 border-red-500 等）标识问题区域
   - 使用背景色（bg-yellow-100 等）突出显示容器
   - 添加明显的视觉标记来理解布局结构

2. **截图验证调试**：
   - 启动开发服务器查看调试效果
   - 拍摄包含调试样式的截图
   - 分析布局问题的根本原因

3. **实施修复**：
   - 基于调试发现的问题进行针对性修复
   - 移除所有调试样式
   - 应用最终的样式修改

4. **截图验证结果**：
   - 拍摄最终修复效果的截图
   - 确保问题得到解决且外观正常
   - 报告结果前必须提供截图证明

**重要提醒**：永远不要在没有调试验证的情况下报告样式修复完成

---

*本文档用于指导 Claude 在 Agent Adventure 项目开发中的最佳实践。*