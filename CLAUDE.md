# Claude 开发指引

## 🚨 诚实性与准确性原则（最高优先级）

### 核心准则：事实优先，绝不说谎

说谎或基于假设报告结果会导致严重后果，必须严格避免。

#### 1. 事实验证优先原则

**在报告任何结果前，必须进行实际验证：**

- ✅ **图像分析**：仔细观察截图的每个细节，逐项验证背景色、文本色、图标状态
- ✅ **代码执行**：确实运行代码并观察输出，不得基于代码逻辑推测结果
- ✅ **功能测试**：实际点击、输入、导航，确认功能真实表现
- ✅ **数据验证**：检查实际数据内容，不得假设数据格式或内容

#### 2. 观察与推论分离原则

**严格区分客观观察与主观推论：**

```markdown
❌ 错误示例：
"主题切换成功，深色模式正常工作"

✅ 正确示例：
"观察到：按钮图标从太阳变为月亮，标签从'浅色'变为'深色'
但是：页面背景色仍然是白色，文本仍然是黑色
推论：设置已更新但CSS样式未应用，需要进一步调试"
```

#### 3. 不确定性诚实原则

**当不确定时，必须明确表达：**

- 使用 "我需要进一步验证..." 而非假装确定
- 使用 "从截图看起来..." 而非断言事实
- 使用 "可能的原因是..." 而非确定诊断
- 主动请求用户确认关键观察

---

## 🔍 API版本与exa MCP查询原则（核心开发流程）

### 1. 开发前必须检查API版本

**任何开发任务开始前，必须执行以下步骤：**

1. **确认使用的技术栈版本**
   - 检查package.json中的依赖版本
   - 查看当前使用的框架版本（React、Next.js、Vue等）
   - 确认API库版本（如axios、fetch等）

2. **使用exa MCP获取对应版本文档**
   ```markdown
   # 示例查询流程
   1. mcp__exa__get_code_context_exa: "React 18.2 hooks API最新用法"
   2. mcp__exa__get_code_context_exa: "Next.js 14 app router API参考"
   3. mcp__exa__get_code_context_exa: "[具体库名] [版本号] 官方文档"
   ```

### 2. 不确定内容必查exa MCP

**遇到以下情况时，必须立即查询exa MCP：**

- 不确定API的正确用法
- 怀疑语法或方法是否过时
- 需要最佳实践参考
- 错误信息无法理解
- 新功能的实现方式不明确

**查询策略：**
- 使用具体的技术栈名称和版本号
- 包含"最新"、"2024"、"官方文档"等关键词
- 查询实际代码示例而非概念解释

### 3. 调试优先使用exa MCP

**调试时的标准流程：**

1. **遇到错误立即查询**
   ```markdown
   mcp__exa__get_code_context_exa: "[错误信息] [技术栈] 解决方案"
   ```

2. **查询调试最佳实践**
   ```markdown
   mcp__exa__get_code_context_exa: "[技术栈] debugging 最佳实践 2024"
   ```

3. **查询相似问题解决方案**
   ```markdown
   mcp__exa__get_code_context_exa: "[具体问题描述] stackoverflow解决方案"
   ```

### 4. 两次失败立即查询原则

**🚨 关键规则：同一个bug修复失败两次时，必须立即使用exa MCP**

**执行流程：**
1. 第一次修复失败 → 分析原因，尝试第二种方法
2. 第二次修复失败 → **立即停止猜测**，执行以下查询：

```markdown
# 必须执行的查询序列
1. mcp__exa__get_code_context_exa: "[错误的完整描述] [技术栈] [版本] 解决方案"
2. mcp__exa__get_code_context_exa: "[具体功能] [技术栈] 常见错误和解决方案"
3. mcp__exa__get_code_context_exa: "[相关概念] 官方文档 最佳实践"
```

3. 基于查询结果重新制定修复策略
4. 如果查询结果仍不足，向用户说明情况并请求帮助

### 5. 深度思考原则

**在每个开发节点都要深度思考：**

#### 5.1 开发前思考
- 这个功能的核心需求是什么？
- 有没有更简单的实现方式？
- 当前技术选型是否最合适？
- 是否需要考虑性能和可维护性？

#### 5.2 实现中思考
- 当前方法是否符合最佳实践？
- 代码是否足够清晰和可读？
- 是否存在潜在的bug或边界情况？
- 异常处理是否完善？

#### 5.3 完成后思考
- 功能是否真正满足需求？
- 代码质量是否达标？
- 是否需要优化或重构？
- 用户体验是否良好？

---

## 📚 exa MCP使用指南

### 常用查询模式

1. **API参考查询**
   ```
   "[库名] [版本] API reference 官方文档"
   "[框架名] [版本] 官方API文档"
   ```

2. **错误解决查询**
   ```
   "[完整错误信息] 解决方案"
   "[技术栈] [错误类型] 常见问题"
   ```

3. **最佳实践查询**
   ```
   "[技术/功能] 最佳实践 2024"
   "[框架] [具体功能] 实现模式"
   ```

4. **代码示例查询**
   ```
   "[具体功能] [技术栈] 代码示例"
   "[技术栈] [功能] 完整实现"
   ```

### 查询质量提升技巧

- 包含具体版本号
- 使用英文关键词提高结果质量
- 包含"official"、"documentation"、"latest"等词
- 查询时优先选择官方文档和权威源

---

## 🛠️ 开发效率原则

### 1. 假设检查模式

任何代码开发都必须使用假设检查模式来确保准确性：

```javascript
// CHECK: 假设这个API返回的数据格式是{id, name, email}
const userData = await fetchUser(id);

// CHECK: 假设useState在这个版本支持函数式更新
const [state, setState] = useState(initialState);
```

**验证流程：**
1. 搜索所有 `// CHECK:` 标记
2. 逐一验证每个假设（查看现有代码、测试功能、使用exa MCP查询）
3. 报告验证结果并修正错误

### 2. 用户操作优势原则

如果某些步骤由用户操作更便利，应该停下来告知用户：
- 外部控制台操作（Supabase、Vercel等）
- 获取API密钥等认证操作
- 文件上传、图片处理
- 第三方服务配置

### 3. 验证结果原则

完成任何开发任务后，必须进行完整验证：

#### 3.1 页面功能验证
- 使用浏览器工具查看页面内容
- 验证用户界面是否正确显示
- 测试交互功能是否正常工作

#### 3.2 截图验证
- 对关键功能进行截图验证
- 对比修复前后的页面效果
- 确保视觉效果符合设计要求

#### 3.3 数据验证
- 检查网络请求是否成功
- 验证数据库查询结果是否正确
- 确认数据传递和转换无误

### 4. Bug修复验证流程

修复bug后必须执行完整的验证流程：

1. **重现问题**：确认bug在修复前确实存在
2. **应用修复**：实施代码修改
3. **功能测试**：验证修复后功能正常工作
4. **回归测试**：确保修复没有引入新问题
5. **用户场景测试**：从用户角度验证完整流程
6. **截图对比**：提供修复前后的可视化证明

### 5. 样式调试原则

修改CSS样式或布局时必须使用调试验证流程：

1. **添加调试样式**：使用彩色边框和背景色标识问题区域
2. **截图验证调试**：启动开发服务器并拍摄调试效果截图
3. **实施修复**：基于调试发现的问题进行针对性修复
4. **截图验证结果**：拍摄最终修复效果的截图

**重要提醒**：永远不要在没有调试验证的情况下报告样式修复完成

---

## ⚠️ 关键提醒

1. **永远不要基于假设编程** - 不确定就查询exa MCP
2. **API版本很重要** - 不同版本API可能完全不同
3. **两次失败必须查询** - 避免无效的重复尝试
4. **深度思考每个决策** - 质量比速度更重要
5. **诚实报告结果** - 不确定的地方明确说明
6. **exa MCP是最佳助手** - 充分利用其强大的代码搜索能力

---

*本指引旨在确保高质量、可靠的代码开发流程。遵循这些原则将显著提高开发效率和代码质量。*